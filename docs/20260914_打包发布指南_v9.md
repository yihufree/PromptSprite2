# 打包发布指南 V9（V2.0.0）

> 本文档对应软件版本 **V2.0.0（2026-09-14）**，Git 标签统一为 `v2.0.0`。
> 由 V8 版修订而来：在 V8（对应 V1.7.0）基础上同步本轮（V2.0.0）的打包与发布要点，更新 EXE 命名示例与 Release 附件名，明确"本轮未新增依赖、`build/PromptSprite.spec` 与打包流程无需改动"，并**新增第 8 章"发布前自检"**。路径示例以 `C:\PSA`（项目根目录）为准，实际发布时替换为你的仓库目录即可。
> 仓库地址：https://github.com/yihufree/PromptSprite

## 目录
1. [打包前准备](#1-打包前准备)
2. [正式打包命令与流程](#2-正式打包命令与流程)
3. [版本号维护与 EXE 命名规则](#3-版本号维护与-exe-命名规则)
4. [打包产物自检清单](#4-打包产物自检清单)
5. [发布到 GitHub Releases](#5-发布到-github-releases)
6. [常见打包问题 FAQ](#6-常见打包问题-faq)
7. [V2.0.0 打包要点更新](#7-v200-打包要点更新)
8. [发布前自检（V2.0.0）](#8-发布前自检v200)

---

## 1. 打包前准备

### 1.1 Python 环境
| 项目 | 要求 | 核对命令 |
|---|---|---|
| Python | 3.12（64-bit） | `python -V` → `Python 3.12.x` |
| 虚拟环境 | 项目根 `.venv`（推荐，打包脚本优先使用它） | 见下 |
| 终端 | 管理员身份打开 PowerShell/CMD | 仅影响开发态运行全局热键，不影响打包本身 |

创建/使用虚拟环境（若尚无 `.venv`）：
```powershell
cd C:\PSA
python -m venv .venv
```
> 说明：`build.py` 会自动优先使用 `C:\PSA\.venv\Scripts\python.exe`（存在时），否则退化为当前解释器。因此**若存在 `.venv`，务必先在其中装好依赖**，避免装错解释器。

### 1.2 依赖安装
```powershell
.\.venv\Scripts\python.exe -m pip install --upgrade pip
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
```
- 运行依赖共 6 项（`requirements.txt` 实测）：`customtkinter / Pillow / pyperclip / openpyxl / keyboard / pystray`（`python-dateutil` 已于 2026-08-18 移除，勿再装回）；
- **V2.0.0 未新增任何第三方依赖**（联网抓取、音频试听、标签/字段管理、结构化向导等全部用 Python 标准库实现），`requirements.txt` 无需改动；
- `pyinstaller` 不必手工装：`python build.py` 第 1 步检测缺失会自动安装。

### 1.3 与打包相关的目录结构（速览）
```
C:\PSA\
├── build.py                            # 一键打包脚本（无命令行参数，自动 7 步，见第 2 章）
├── requirements.txt                    # 运行依赖（6 项）
├── app\config.py                       # APP_VERSION / BUILTIN_* 版本 / 目录常量（版本号唯一来源）
├── app\resources\builtin_manual.md     # 内置手册（约 90 条，版本串见 1.5）
├── app\resources\builtin_prompts.db    # 内嵌完整库（打包时由 build.py 从 data\prompts.db 同步生成）
├── data\prompts.db                     # ★开发态主库＝发布数据唯一来源
├── Icons\PSicon.png                    # 图标源（打进 EXE，托盘/窗口用）
├── build\PromptSprite.spec             # PyInstaller 配置（onefile + windowed）
├── build\generate_icon.py              # PSicon.png → build\app.ico
├── build\build.bat                     # 备选批处理（仅快速验证用，见 2.2 的局限）
└── dist\                               # 产物目录（版本化 EXE + 可选 data\）
```
> 数据量参考（2026-09-09 对 `data\prompts.db` 实测）：**2556 条目 / 173 分类 / 11 根目录 / 4 项目类别**。README 徽章写的"2555 条精简模板"是历史口径；**发布前以打包当日 `data\prompts.db` 实际数据为准**（首次建库后"关于/状态栏"条数与内置库一致即可，见第 4 章）。

### 1.4 内置模板库同步（data\prompts.db → builtin_prompts.db）
机制（对应 `build.py::_prepare_builtin_db`，2026-08-18 第 023 条）：
- **每次 `python build.py` 第 4 步自动执行**：把开发态最新主库 `data\prompts.db` 覆盖复制为 `app\resources\builtin_prompts.db`（spec 的 `datas` 引用它，随 EXE 内嵌）；
- 打包态 EXE 首次运行（exe 旁尚无 `data\prompts.db`）时，由 `main.py::_ensure_initial_db()` 把该内嵌库复制到 exe 同目录 `data\`，做到"开箱即用最新数据"；**已有 data 文件时绝不覆盖**（保护用户数据）；
- 若 `data\prompts.db` 不存在，脚本只告警不报错——此时 EXE 将不含完整库，仅剩内置手册导入流程，属异常发布，务必避免。

手工同步等价命令（仅在跳过 build.py 直接 PyInstaller 时使用）：
```powershell
Copy-Item -Force C:\PSA\data\prompts.db C:\PSA\app\resources\builtin_prompts.db
```
同步前确认（防呆检查）：
1. `data\prompts.db` 已通过迁移向导完成根目录/项目类别归属，无"未分配"状态；
2. **该库已按隐私要求清理**：不含个人/机密内容（含历史删除留在回收站 trash 表或备份中的快照——发布前需确认是否应清除，见 FAQ-3）；
3. 同步后可对比两文件大小一致、用 SQLite 数一遍条数，确认与发布说明一致。

### 1.5 内置手册版本机制（BUILTIN_MANUAL_VERSION）与注意点
代码位置：`app\config.py` `BUILTIN_MANUAL_VERSION = "007-2026-08-18"`；启动逻辑在 `app\main.py::main()` 第 4 步：
- 每次启动读取 `meta.builtin_manual_version`，**仅当与 `BUILTIN_MANUAL_VERSION` 不一致时**才触发一次内置手册导入（进度条窗口），完成后把该版本号写回 meta；
- 导入为 **"非破坏性合并"**（2026-08-18 P0-2 修复）：分类按名复用、条目按名 upsert，**不会清空任何现有数据**——所以"改版本号→重导入"对老用户是安全的。

注意点（打包人必读）：
1. **改了 `builtin_manual.md` 内容却忘了升 `BUILTIN_MANUAL_VERSION` → 老安装包不会重新导入**（meta 仍是旧值，等于白改）；
2. 反之只要升版本号（如改为 `"008-2026-09-09"`），老用户下次启动会自动触发合并导入；
3. 版本串无固定格式，但务必**整串变更**且与 meta 历史值不同，习惯写法 `"编号-YYYY-MM-DD"`；
4. 内嵌完整库与手册是两套互补机制：全库负责"新用户开箱即用全量数据"，手册负责"版本升级时的增量合并补丁"。若某轮数据全量进了 `data\prompts.db`（会随 build 内嵌），新装用户直接拿全库，无需再靠手册。

### 1.6 打包前回归测试（必做，全过才允许打包）
```powershell
# ① 统一自测入口：数据库 / 备份 / 变更包(增量) / MD 解析器，全部模块（不开 GUI）
python -m app.self_test

# ② 主界面 GUI 自动化回归（需桌面环境，V1.5.0 起维护）
python -m app.gui_self_test

# ③ GUI 冒烟：启动数秒后自动退出（退出码 0 即通过）
python -m app.main --smoke
```
> V2.0.0 自测须覆盖本轮新逻辑（schema v4 六张新表 / 标签读写与标签页面 / 图集（封面+图集、下载到本地、清空回收站释放图集文件）/ 字段定义与 11 种字段类型 / 列表框三类数据源与失联占位 / 结构化向导（四层映射、质量自检、5-d 四项、先存后导）/ 联网抓取（多通道降级、限额、表头合并）/ JSON v5 与旧 v2/v3/v4 兼容 / 变更包携带 field_defs 与字段差异确认 / 打标签与改自定义字段刷新时间戳）；发布前请复跑一遍，不要沿用旧结论。

---

## 2. 正式打包命令与流程

### 2.1 推荐方式：一键打包（唯一正规流程）
```powershell
cd C:\PSA
python build.py            # 若 .venv 存在会自动使用 .venv\Scripts\python.exe
```
`build.py` 自动完成 7 步（对照源码 `build.py::main()` 逐一核对，无需手工分步）：

| 步 | 动作 | 对应实现 |
|---|---|---|
| 0 | 选定解释器：优先 `.venv\Scripts\python.exe`，否则当前解释器 | `PY` 变量 |
| 1/7 | `pyinstaller` 缺失则自动 `pip install pyinstaller` | `main()` step 1 |
| 2/7 | `pip install -r requirements.txt`（每次执行，幂等） | step 2 |
| 3/7 | `build\generate_icon.py` 由 `Icons\PSicon.png` 生成 `build\app.ico` | step 3 |
| 4/7 | **同步内置完整库**：`data\prompts.db` → `app\resources\builtin_prompts.db` | `_prepare_builtin_db()` |
| 5/7 | `python -m PyInstaller --noconfirm --clean build\PromptSprite.spec`（在项目根执行，产物 `dist\PromptSprite.exe`） | step 5 |
| 6/7 | **更新 EXE 旁数据目录**：复制 `data\prompts.db` → `dist\data\prompts.db`（只动该文件，保留 dist\data 下已有 images/backup 等） | `_update_dist_data()` |
| 6.5 | **按规则重命名产物**为 `PromptSprite_<YYMMDD>_V<版本号>.exe`（同名先删再改，防 PyInstaller 占用） | `versioned_exe_path()` + rename 段 |
| 7/7 | 校验产物存在并打印大小 | step 7 |

成功标志（示例）：
```
=== 打包完成：dist\PromptSprite_260914_V2.0.0.exe（体积以实际输出为准）===
```
> **重要**：现行 `build.py` **不接受任何命令行参数**。V7 文档中的 `--onefile / --onedir / --output-dir / --sync-db / --smoke` 等选项**均已不再需要、也未实现**；请勿照 V7 执行。规格固定为 onefile + windowed（见 spec），不存在 onedir 产物。

### 2.2 备选方式：build.bat / 手工 PyInstaller（仅快速验证，不建议用于正式发布）
`build\build.bat` 只做三件事：生成图标 → `PyInstaller --noconfirm --clean build\PromptSprite.spec` → 列出产物。**局限必须知晓**：
- 不自动同步 `builtin_prompts.db`（若该文件缺失，spec 因 datas 找不到文件直接报错；若陈旧，EXE 携带旧库）；
- 不更新 `dist\data\prompts.db`；
- 不改名（产物仍是裸 `dist\PromptSprite.exe`）；
- 假设存在 `.venv`。

手工等效流程（必须补上它缺的步骤）：
```powershell
# 0) 先保证内嵌库最新（缺一不可）
Copy-Item -Force data\prompts.db app\resources\builtin_prompts.db
# 1) 生成图标（app.ico 已入库可跳过，改了 PSicon.png 才需重跑）
.\.venv\Scripts\python.exe build\generate_icon.py
# 2) PyInstaller 打包（在项目根目录执行）
.\.venv\Scripts\python.exe -m PyInstaller --noconfirm --clean build\PromptSprite.spec
# 3) 手工改名（见第 3 章规则）
Move-Item dist\PromptSprite.exe dist\PromptSprite_260914_V2.0.0.exe -Force
```

### 2.3 spec 收集内容核对（build\PromptSprite.spec）
| 类型 | 内容 | 去向（EXE 内） | 用途 |
|---|---|---|---|
| datas | `app\resources\builtin_manual.md` | `resources\` | 内置手册（首次/版本变更导入） |
| datas | `app\resources\builtin_prompts.db` | `resources\` | 内置完整库（首次运行复制到 exe 旁 data\） |
| datas | `Icons\PSicon.png` | `Icons\` | 托盘/窗口图标（打包态 `hotkey.py` 读 `sys._MEIPASS\Icons\PSicon.png`） |
| icon | `build\app.ico` | — | EXE 文件图标 |
| 入口 | `app\main.py` | — | 程序入口 |
| excludes | `matplotlib/numpy/pandas/scipy/IPython/jedi` | — | 防误收大模块，控制体积 |
| 其它 | `name=PromptSprite`、`console=False` | — | 单文件、无控制台窗口 |

**关键核对点：没有任何 `data\` 目录被收进 EXE**（spec 的 `datas` 只有上面三项）。`data\`（含用户库/备份/变更包/图片）只存在于 exe 同目录，运行时自动生成/复用。请勿为了"省事"把 `data` 加入 datas——那会把开发机的用户数据与备份打进每个安装包，是隐私事故。

### 2.4 dist 目录的默认行为
- PyInstaller 每次输出裸 `dist\PromptSprite.exe`；`--clean` 只清 `build\` 临时目录，**不清 dist**；
- `build.py` 第 6 步会补 `dist\data\prompts.db`（便于整个 dist 拷走即开箱），**且保留 dist\data 下既有 backups/变更包**——若 dist\data 里残留旧备份，会形成隐私泄漏点（见 FAQ-3/4）；
- 同一台机同一天多次打包：同名目标先删后改，最终只留一个当日版本化 EXE；但**历史遗留的不同版本/不同命名 EXE 不会被自动清理**（当前 `dist` 下实测仍有 `PromptSprite_1.3.0.exe`、`PromptSprite_1.5.0.exe`、`PromptSprite_260907_V1.6.0.exe` 及带旧备份的 `dist\data`）；
- **正式发布前建议：删除 dist 全部旧文件（或整个 dist 目录）后重新一键打包**，保证产物目录干净、无隐私残留。

---

## 3. 版本号维护与 EXE 命名规则

### 3.1 APP_VERSION 唯一来源：app\config.py
```python
APP_VERSION = "2.0.0"   # 2026-09-14：标签系统/图集/自定义字段与 11 类型/网上资源结构化向导/联网抓取 + 数据安全修复（注释随版本更新）
```
维护规则：
- 只改这一处；`build.py` 靠正则从该文件读版本号用于命名；
- **必须使用英文双引号** `"2.0.0"`。`build.py::_load_version()` 的正则是 `APP_VERSION\s*=\s*"([^"]+)"`，若误写成单引号 `'2.0.0'`，匹配失败将回退为 `0.0.0`，产物会变成 `..._V0.0.0.exe`；
- 版本号要同步到：README 徽章/更新日志、说明书、需求规格、"设置→关于"（读取同一 config，自动一致）、本指南脚注、Release Notes；
- EXE 命名里的"版本号"来自此常量，"日期"来自打包机系统当天日期（`datetime.now()`），两者拼接即为规则名。

### 3.2 EXE 命名规则（2026-09-07 第 7 条起执行）
```
PromptSprite_<最后修改日期YYMMDD>_V<版本号>.EXE
```
示例：`PromptSprite_260914_V2.0.0.EXE`（2026-09-14 打包、V2.0.0）。
> 文件系统上实际扩展名为小写 `.exe`（Windows 不区分大小写），文档/Release 中习惯写作 `.EXE` 等价。日期 YYMMDD＝打包当日（"最后修改"即生成那天），与 GitHub Release 当天日期一致最稳妥。

### 3.3 build.py 重命名逻辑核对点（打包前逐条核）
对照 `build.py` 源码检查以下四处，任何一处与描述不符都需先修脚本再打包：
1. `_load_version()`：正则 `APP_VERSION\s*=\s*"([^"]+)"` 在 `app\config.py` 中应恰好命中一处且取到 `2.0.0`（可临时加 `print(_load_version())` 验证）；
2. `versioned_exe_path()`：拼 `dist\PromptSprite_{datetime.now():%y%m%d}_V{version}.exe`——确认用的是**当天日期**与**config 版本**，无写死版本/日期；
3. 重命名段：`os.path.isfile(EXE_OUT)` 失败要 `sys.exit(1)`；目标同名文件先 `os.remove` 再 `os.rename`（防占用）；改名后第 7 步打印的应是**版本化路径**而非裸 `PromptSprite.exe`；
4. 幂等性：同一天连续两次 `python build.py`，第二次应在改名时先删掉第一次的同名产物再生成，最终 dist 只有一个当日 EXE（旧版本 EXE 不受影响，需手动清理，见 2.4）。

---

## 4. 打包产物自检清单

拿到 `dist\PromptSprite_260914_V2.0.0.EXE` 后，**复制到一个干净空目录**（如 `D:\ReleaseTest\`）再运行，逐项实测（不要直接在 dist 里测，避免污染/误判首次建库）：

| # | 检查项 | 通过标准 |
|---|---|---|
| 1 | **首次建库** | 空目录双击 EXE：exe 旁自动生成 `data\prompts.db`，体积与内置库相当；状态栏条目/分类/根目录计数正确；不弹任何"资源缺失"错误 |
| 2 | **关于与版本** | "设置→关于"显示 `V2.0.0`、**首行名称显示 `PromptSprite2`**、开源仓库 `https://github.com/yihufree/PromptSprite`；条目数/数据说明与发布口径一致 |
| 3 | **菜单导入导出** | 导出 JSON/Excel/HTML 各一次成功；再把 JSON 导回（判重去重生效）；Excel 导入一份样本正常；**旧版本 JSON(v2/v3/v4)、旧 Excel 模板、MD 手册**也能导入 |
| 4 | **变更包生成** | 新增/修改/删除几条后正常退出 → `data\backup\incremental\` 出现 `变更包_{电脑代号}_{日期}_addN_delM.json`，N/M 计数正确；当天反复退出只保留 1 个文件 |
| 5 | **变更包导入安全** | "导入变更包…"能预览（来源/新增/删除计数）；默认"仅合并新增/修改"；选"完整同步"须输入确认短语；执行前生成 `prompts_preimport_*.db`；无快照包时"⑤逆向恢复"置灰 |
| 6 | **备份目录** | `data\backup\prompts_YYYY-MM-DD_*.db` 按天去重、默认保留 ≤30 份；手动快照（`snapshot_*` 等）不被自动清理 |
| 7 | **删除 / 回收站** | 删除条目 → 详情底部"删除历史/回收站"可见并可恢复（内容/位置回挂）；彻底删除/清空才释放图片（**含图集本地文件**，不留孤儿文件）；**回收站批量入站的快照含标签/自定义字段/图集** |
| 8 | **数据比对** | "数据比对…"选一个备份 `*.db`：差异概览/明细正常，可导出 HTML 差异报告；全程不动主库 |
| 9 | **一键导出** | "导出当日变更包到…"弹摘要（新增/删除披露）；纯删除包显示"⚠ 负增量"警示 |
| 10 | **旧库迁移不被破坏** | 把旧版库（含 v1/v2 时代结构或未归属根目录）放入 data 覆盖 prompts.db 再启动：迁移向导可用、四级归属正确、原条目/分类不丢；导入/导出/备份全链路照常 |
| 11 | **热键与托盘** | 管理员（或白名单）下 `Ctrl+Shift+P` 唤出搜索、托盘恢复/退出正常；非管理员注册失败只提示、不影响其它功能 |
| 12 | **换机数据完整性**（可选） | 复制整套 data 到另一台机器打开：计数一致、无报错 |
| 13 | **标签系统与标签页面** | 详情区 🏷 可增删标签且输入自动补全既有标签；标签页面（工具栏 + 详情区底部两入口，与四级目录**互斥**）可打开；标签云 ⇄ 列表＋计数切换且记住上次选择；且/或过滤、按名称/计数排序、重命名/合并/删除/清理未使用可用；搜索框 `#标签` 可筛出该标签条目 |
| 14 | **条目图集（封面 + 图集）** | ⑦ 区可添加**本地图 / 外链图**、排序、移除；外链图"下载到本地"成功落盘、失败仍保留外链与来源网址；新建态选图后放弃保存不产生孤儿文件 |
| 15 | **自定义字段与 11 种类型** | 字段管理可对自定义字段 增 / 删（归档隐藏）/ 改名 / 排序 / **改类型**；内置 10 项可改名、不可删、不可改类型；11 种类型控件与校验可用（数字保存校验、日期 `YYYY-MM-DD`、是·否勾选、列表框、标签文本、附件/短音频（可试听）/图像"选择+打开"）；列表框三类数据源（序列/目录层级/条目）与失联占位"该项已经被删除（原：…）"正常 |
| 16 | **网上资源结构化向导** | 「⇩ 导入 → 网上资源结构化」三步可用；三选源（粘贴文本 / 本地文件 CSV·TSV·TXT·HTML·MD / 从网址获取）；四层映射（空单元格继承、跳层兜底）+ 字段映射；质量自检面板七项有数；出口"先保存数据文件（JSON v5）再导入"，导入前生成快照、失败可回滚；5-d 四项（行筛选 / 关键词规则→二级分类 / 中英分流 / 同时导出 Excel）可用 |
| 17 | **联网抓取与 GitHub 批量** | 抓取状态行显示 `HTTP / 类型 / 体积 / 识别结果 / 通道`；多通道降级链正常；多表页面"选用表格"下拉可用；相对链接补全可开关；GitHub 目录批量：默认勾选常见文档、显示体积、限额（单文件 ≤2MB / 一次 ≤20 个 / 合计 ≤20MB）生效、个别失败逐个跳过、表头一致自动合并 |

全部通过后，记录：EXE 文件名、文件大小（MB）、SHA256，写入 Release（见第 5 章）。任一失败即回到源码修复后重新打包，**不要带病发布**。

---

## 5. 发布到 GitHub Releases

仓库：https://github.com/yihufree/PromptSprite

### 5.1 Git 提交与标签
```bash
git add -A
git commit -m "v2.0.0: 标签系统与标签页面 + 条目图集 + 自定义字段与11种类型 + 网上资源结构化向导(含联网抓取/GitHub批量) + 数据安全修复 + schema v4 / JSON v5"
git tag -a v2.0.0 -m "PromptSprite v2.0.0（2026-09-14）"
git push origin main --tags
```
> 提交前确认：`APP_VERSION="2.0.0"`、README/说明书/需求规格已同步 V2.0.0、内置库与手册为最新、自测全绿、EXE 已按第 4 章自检。

### 5.2 创建 Release（基于 v2.0.0 标签）
- Tag：`v2.0.0`；标题：`PromptSprite V2.0.0（2026-09-14）`
- 附件（必传）：
  - `dist\PromptSprite_260914_V2.0.0.EXE`（主安装包）；
  - `PromptSprite_260914_V2.0.0.EXE.sha256`（校验文件）；
  - （可选）README/LICENSE/docs 说明或整目录 zip——**zip 内严禁夹带开发机 data/backup、testtmp、快照等隐私文件**（见 FAQ-3）。

### 5.3 Release Notes 建议模板（可直接套用）
```markdown
## PromptSprite V2.0.0（2026-09-14）

📦 下载：PromptSprite_260914_V2.0.0.EXE（体积与 SHA256 见附件 .sha256）
🔒 数据仅存本机，默认离线；仅在结构化向导中主动点"从网址获取"时才会联网下载你指定的网页/GitHub 文件，不上传任何数据。
⚙ 库结构 schema v4、导入导出 JSON v5，均为纯新增且向后兼容：旧备份、旧导出文件（JSON v2/v3/v4、旧 Excel 模板、MD 手册）照常导入，数据文件名一律未改、无需迁移。

### 本版更新
- 标签系统：详情区 🏷 标签区块（自动补全 / 回车新建 / × 移除）；标签页面（工具栏 + 详情区底部两入口，与四级目录互斥），
  标签云 ⇄ 列表＋计数徽标双呈现（记住上次选择）、且/或组合过滤、按名称/计数排序、重命名/合并/删除/清理未使用；
  设置项"在条目处显示标签"（默认关）；全局搜索支持 #标签 语法。
- 条目图集：详情区 ⑦ 升级为"封面 + 图集"，本地图/外链图双源、排序、移除，外链图可"下载到本地"（失败保留外链与来源网址）。
- 自定义字段与 11 种字段类型：字段管理支持自定义字段 增/删（归档隐藏）/改名/排序/改类型；内置 10 项可改名、不可删、不可改类型；
  单行文本/多行文本/链接/列表框/数字/日期/是·否/标签/附件/短音频（可试听）/图像；列表框三类数据源（自定义序列/目录层级/条目）。
- 网上资源结构化向导：「⇩ 导入 → 网上资源结构化」；三选源（粘贴文本/本地文件/从网址获取）→ 四层映射 + 字段映射 → 树预览 + 质量自检 → 先保存数据文件再导入；
  含行筛选、关键词规则→二级分类、中英提示词自动分流、同时导出 Excel 四项增强。
- 联网抓取（仅主动使用时联网）：多通道降级（raw → GitHub contents API → git blob API）、多表"选用表格"、相对链接自动补全、抓取状态行；
  GitHub 目录批量多选（默认勾选常见文档、显示体积、限额 单文件≤2MB / 一次≤20 个 / 合计≤20MB），个别失败逐个跳过。
- 数据安全修复：回收站批量入站快照补全标签/自定义字段/图集；清空回收站同步释放图集本地文件；变更包随包携带字段定义并做字段差异逐项确认；
  打标签、改/删自定义字段会正确刷新条目时间戳，从而进入当日变更包。
- 界面修复：清空搜索框回到进入搜索前的视图；关闭标签页面两条路径行为统一；详情显示模式（自动/全部/精简）真正生效
  （默认"自动"下仅"视频/图像"两根目录完整显示 ②~⑦）；工具栏最小宽度约 1496（四列全隐藏约 1104）。
- 显示名：仅主窗口品牌标签与"设置 → 关于"窗口首行改为 `PromptSprite2 · 提示精灵`，其余名称保持 PromptSprite。

### 旧版本回顾
- V1.9.0：详情区字段浮动提示窗口重构 + 💬 浮动提示总开关；
- V1.8.0：工具栏重排 / 目录隐藏显示 / 编辑·浏览切换 / 搜索"停顿才查"等界面修订；
- V1.7.0：变更包(v4) + 导入安全向导 + 数据比对 + 携带被删快照与逆向恢复。

### 使用提示
- 全局热键 Ctrl+Shift+P 需以管理员运行或加入杀软白名单（失败不影响其他功能）；
- 首次运行会新建 data\ 目录；升级不会覆盖/清空你的数据。
- 联网抓取不支持需 JS 渲染的页面、不处理登录、不做风控绕过、不做站内遍历；部分站点需补 Referer/Cookie。
```

### 5.4 发布前最终核对清单
1. 自测/冒烟全部退出码 0（见第 8 章）；2. EXE 已按第 4 章 17 项自检通过；3. 内置库为打包当日最新快照且无隐私残留；4. 记录 EXE 文件名/大小/SHA256（**以实际输出为准**）；5. dist 目录已清理（无旧 EXE、无旧 data 残留）；6. Release Notes 与实际变更一致；7. 附件齐全（EXE + sha256）。

---

## 6. 常见打包问题 FAQ

**Q1：打包/运行时提示缺少模块（ModuleNotFoundError: customtkinter / PIL / pystray…）**
- 开发态：确认装了依赖的解释器就是运行的解释器——统一用 `.venv`；`python build.py` 第 2 步会自动装；
- 打包态：纯 Python 模块一般由 PyInstaller 自动收集；若仍缺，先看 `build\PromptSprite\warn-PromptSprite.txt`（PyInstaller 每次打包都会生成，列出未收集到的 import 警告），确属漏收的在 spec `hiddenimports` 里补；
- 新 UI 模块（如 V2.0.0 的 `import_wizard_dialog.py` / `github_batch_dialog.py` / `field_manager_dialog.py` / `field_defs_diff_dialog.py`，以及 V1.7.0 的 `change_import_dialog.py` / `compare_dialog.py`）均被 `main_window.py` 或其它对话框 import，PyInstaller 静态扫描可收集；个别在函数内延迟 import 的（compare/history、部分对话框）也建议打包后实测对应入口能打开。
- 抓取/解析等纯标准库新模块（`app/parser/fetcher.py`、`app/parser/hierarchy_import.py`）无第三方依赖，随包自动收集；**无需新增 spec datas 或 hiddenimports**。

**Q2：杀毒误报 / SmartScreen / 全局热键不灵**
- onefile + UPX（spec `upx=True`）+ 无签名是误报高发组合；发布时可附 sha256 供核对；仍被拦截建议用户"添加信任/白名单"；
- 全局热键 `Ctrl+Shift+P` 使用 keyboard 钩子：**需管理员身份运行或加杀软白名单**；注册失败只弹提示，不影响主功能（属设计行为）；
- 可选：用代码签名证书对 EXE 签名（如 osslsigncode），可显著降低 SmartScreen 拦截，非必需。

**Q3：data 残留与隐私数据检查（发布前必查）**
- `data\` 永远不会被打进 EXE（spec 无此收集项），但**开发机上的 data 会经 build.py 第 6 步同步出 dist\data**；若把整个 dist 打包分发，风险点如下，发布前逐项删除：
  1. `data\backup\*.db` 旧全量备份（如 `snapshot_清理前_*.db` 是含隐私的历史快照，实测仍残留在开发机）；
  2. `data\backup\incremental\*.json` 旧"增量_*"或含他人数据的"变更包"（实测 dist 下仍残留 `增量_DESKTOP-hahaha_2026-09-07.json` 等）；
  3. `Dev_Work_Log\testtmp\` 等测试副本（含 prompts.db / prompts-error.db / 增量 json）；
  4. 回收站 trash 表与 deletion_log.payload（V1.7.0 起删除日志可能带完整快照）——若这些是隐私清理的产物，发布前应在开发库执行"彻底删除/清空回收站"后再同步内置库。
- 干净做法：删除整个 `dist` → 重新 `python build.py` → 仅取版本化 EXE 发布；如需附带初始 data，用全新空目录首次运行生成的干净 data。

**Q4：dist 目录清理**
- `--clean` 只清 PyInstaller 缓存（build\），**不清 dist**；dist 下旧 EXE（实测有 `PromptSprite_1.3.0.exe`、`PromptSprite_1.5.0.exe`、`PromptSprite_260907_V1.6.0.exe`）与旧 `dist\data` 需手动删除，否则会把过期版本/隐私数据一起发出去。

**Q5：EXE 体积**
- 体积主要来自 customtkinter/Pillow/解释器与内嵌库；spec 已 excludes 掉 numpy/pandas/matplotlib 等大模块；控制内置库体积即可维持体量；UPX 压缩已开启。
- **V2.0.0 未新增任何第三方依赖**（新功能全部用标准库实现），因此 **EXE 体积预计基本不变**（历史参考：V1.6.0 约 22.8 MB）；**实际体积以打包当日输出为准，文档不写死数字**（见第 8 章自检记录）。

**Q6：发布后发现 EXE 内置数据不是最新**
- 原因通常是绕过了 `python build.py`（如直接用 build.bat/手工 PyInstaller），导致 `builtin_prompts.db` 未同步。正规流程第 4 步每次强制覆盖；手工流程务必先补 `Copy-Item data\prompts.db app\resources\builtin_prompts.db`。

**Q7：产物文件名是 V0.0.0 / 无日期版本**
- 见 3.1：`APP_VERSION` 引号必须是英文双引号且恰好一处匹配；改完 config 后重跑 build.py 验证第 6.5 步打印的版本化路径。

**Q8：EXE 首次运行报"内置手册导入失败/无法读取内置手册"或建库为空**
- 多为 spec datas 路径与 `config.builtin_manual_path()/builtin_db_path()`（打包态读 `sys._MEIPASS\resources\`）不一致；核对 spec 三项 datas 均存在且文件名拼写一致，重新打包。开发态不会复现（读 `app\resources\`），务必按第 4 章在空目录实测。

---

## 7. V2.0.0 打包要点更新

### 7.1 本轮改动与资源影响核对（以实际代码为准）

已对照源码逐项核对（2026-09-14）：

| V2.0.0 改动 | 涉及文件 | 是否新增打包资源 |
|---|---|---|
| 库结构 schema v4：6 张新表（field_defs / entry_field_values / entry_ref_links / tags / entry_tags / entry_images） | `app\database.py` | 否（库结构运行时自动迁移，与内嵌库无关） |
| 标签系统 + 标签页面 | `app\database.py`、`app\ui\main_window.py` | 否（纯 Python） |
| 条目图集（封面 + 图集、下载到本地） | `app\database.py`、`app\ui\main_window.py` | 否 |
| 字段管理与 11 种字段类型 | `app\ui\field_manager_dialog.py`（新增）、`app\ui\main_window.py`（字段控件） | 否（纯 Python，自动收集） |
| 字段定义差异确认（新 UI） | `app\ui\field_defs_diff_dialog.py`（新增） | 否（纯 Python，自动收集） |
| 结构化向导（改造） | `app\ui\import_wizard_dialog.py`、`app\parser\hierarchy_import.py`（新增） | 否 |
| GitHub 目录批量（新 UI） | `app\ui\github_batch_dialog.py`（新增） | 否（纯 Python，自动收集） |
| 联网抓取层（纯标准库） | `app\parser\fetcher.py`（新增） | 否（无第三方依赖） |
| JSON v5（四层路径、field_defs 随包、标签/自定义字段/图集往返，兼容 v2/v3/v4） | `app\parser\json_io.py` | 否 |
| Excel 追加列（自定义字段 / 标签 / 图集 / 项目类别）+ 载荷导出 | `app\parser\excel_io.py` | 否 |
| 变更包补 field_defs；载荷含标签 / 自定义字段 / 图集 | `app\incremental_backup.py` | 否 |
| 数据安全修复（回收站快照补全、清空释放图集文件、时间戳刷新） | `app\database.py` | 否 |
| 版本号与显示名常量 | `app\config.py` | 否 |

**结论：V2.0.0 全部为纯 Python 代码改动，未引入任何新二进制/外部资源；`requirements.txt` 无新增依赖；`build\PromptSprite.spec` 的 datas（内置手册 / 内置库 / 图标）与 icon 路径无需改动，spec 文件名与 EXE 前缀也未改名。** 与打包相关的动作仅为：确认 `app\config.py` 的 `APP_VERSION` 已为 `"2.0.0"`（`BUILTIN_MANUAL_VERSION` 若本轮手册有改动则同步升级版本串）。

### 7.2 打包动作清单（V2.0.0）
1. 跑 `python -m app.self_test` + `python -m app.gui_self_test` + `python -m app.main --smoke`，全绿；
2. 核对 config：`APP_VERSION="2.0.0"`、`BUILTIN_MANUAL_VERSION` 版本串、备份/变更包相关常量；
3. 清理 dist 旧产物与隐私残留（FAQ-3/4）→ `python build.py`；
4. 按第 4 章 **17 项**自检（重点：标签系统与标签页面、条目图集与下载到本地、自定义字段与 11 类型、结构化向导三选源与质量自检、联网抓取状态行与多通道降级、GitHub 批量限额、旧 v2/v3/v4 JSON 导入、变更包字段定义差异确认）；
5. Git 提交 + `v2.0.0` 标签 + Release（第 5 章）。

### 7.3 人工核对提示
> **请人工核对 build 配置后再打包**：正式发布前，请打开 `build\PromptSprite.spec` 复核三项 datas（`builtin_manual.md` / `builtin_prompts.db` / `PSicon.png`）与 icon 路径存在且与上表结论一致；并确认打包机的 `data\prompts.db` 就是你想随 EXE 发布的快照（条数、隐私清理状态）。核对无误后再执行 7.2。

---

## 8. 发布前自检（V2.0.0）

打包前后按下表逐项执行，**全部通过才允许发布**。命令统一在项目根目录（示例 `C:\PSA`）执行；找不到 `.venv` 时用当前解释器。

### 8.1 三步自测（必须全绿）
```powershell
# ① 统一自测入口：数据库 / 备份 / 变更包(增量) / MD 解析器 等全部数据层模块（不开 GUI）
python -m app.self_test

# ② GUI 冒烟：启动数秒后自动退出（退出码 0 即通过）
python app/main.py --smoke

# ③ 主界面 GUI 自动化回归（需桌面环境）
python -m app.gui_self_test
```
> 说明：三步**全部退出码 0** 方可继续；本轮自测须覆盖第 7.1 表与 1.6 节列出的 V2.0.0 新逻辑。若测试脚本等价入口是 `python -m app.main --smoke`（而非 `python app/main.py --smoke`），两者效果一致，按仓库实际可用写法执行。

### 8.2 打包产物记录（以实际输出为准）
1. 在**干净空目录**里按第 4 章 17 项自检（首次建库、关于与版本、导入导出、变更包、回收站、标签/图集/字段、结构化向导、联网抓取等）；
2. 记录产物信息（**不要抄旧文档里的数字，全部以本次实际输出为准**）：
   - **EXE 文件名**：`dist\PromptSprite_<YYMMDD>_V2.0.0.exe`；
   - **EXE 体积（MB）**：以 `Get-Item` / 资源管理器实测为准，文档不写死数字；
   - **SHA256**：以实测为准，例如
     ```powershell
     Get-FileHash dist\PromptSprite_260914_V2.0.0.exe -Algorithm SHA256
     ```
     并把哈希写入 Release 附件的 `.sha256` 文件（见 5.2）；
3. 把文件名、体积、SHA256 与自检结论一并写入发布记录 / Release Notes。

### 8.3 发布前最终门禁（与 5.4 呼应）
1. `python -m app.self_test`、`python app/main.py --smoke`、`python -m app.gui_self_test` 全部退出码 0；
2. EXE 已按第 4 章 17 项在干净目录自检通过；
3. `app\config.py` 的 `APP_VERSION="2.0.0"`，`build\PromptSprite.spec` 无意外改动；
4. dist 目录已清理（无旧 EXE、无旧 `dist\data` 隐私残留）；
5. 已记录 EXE 文件名 / 体积 / SHA256（**以实际输出为准**）；
6. README、说明书（V10）、本指南（V9）版本号与发布口径一致；
7. 附件齐全（EXE + sha256），Release Notes 与实际变更一致。

---

*文档版本：2026-09-14 V9（对应软件 V2.0.0）*
*维护者：PromptSprite 开发组*
