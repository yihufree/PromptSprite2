# -*- coding: utf-8 -*-
import os

note = """

---

## ⚠ 已知限制与说明（2026-09-09，V1.7.0 复核补充）

1. **多位置条目的 JSON/Excel 导出往返**：同一分类子树导出时，多位置（主挂靠 + 关联）条目会在文件里出现多行，再次导入会变成多份独立副本而非原"关联关系"（B-13 记录；如需保真请用整库 JSON 或保留多位置功能原地使用）。
2. **数据比对口径**：条目差异按"详情内容键（content_key）"识别；同名不同内容会被视为不同条目，内容一致但位置不同的两条视为同一条（已按分类 id 修复同名分类互相覆盖问题）。
3. **"修改"的换机同步语义**：当前以内容判重合并，"修改"表现为"新增一条 + 原条保留"；真正的"更新/编辑同步"需引入稳定条目 ID（后续增强）。
"""
path = os.path.join(os.getcwd(), "README.md")
with open(path, "a", encoding="utf-8") as f:
    f.write(note)
print("README note appended")
